// var sayHello = function(){

// }

// window.sayHello();
// // As sayHello is defined globally, if we will write the same function in the new module 
// // then that new function will override the previous one. So avoid defining the function in global scope.

console.log(module);
// It appears to be a global object but actually it is not.